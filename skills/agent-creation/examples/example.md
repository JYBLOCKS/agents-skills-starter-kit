# agent-creation Example

## Input

Create an agent for release coordination with explicit triggers and handoff rules.

## Output

- normalized folder path
- all six drafted agent files
- validation against `agents/CONTRACT.md`
