# context-creation Example

## Input

Create a reusable context file for partner integration conventions.

## Output

- normalized `context/<name>.md`
- reusable context draft
- validation around owner, consumers, and freshness
