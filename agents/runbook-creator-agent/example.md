---
name: runbook-creator-agent-example
description: Example invocation and output shape for runbook-creator-agent
trigger: Use this when you need a concrete example of how runbook-creator-agent should be invoked or how it should respond
metadata:
  author: starter-kit
  version: 0.3.0
  scope: Owns a reference example for runbook-creator-agent, but does not redefine the canonical contract
  auto-invoke: false
tools: shared/tool-permissions.md#standard-tool-set
---
# runbook-creator-agent Example
# Phase 1: IntentThe user wants a runbook for onboarding a new coding tool.
# Phase 2: Contract CompletionThe creator asks about trigger, prerequisites, step order, expected outputs, rollback, and escalation.
# Phase 3: Assembly and ValidationThe output is a complete runbook draft with only the sections that the workflow actually needs.
