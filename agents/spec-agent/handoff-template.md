---
name: spec-agent-handoff-template
description: Standard downstream handoff template for spec-agent
trigger: Use this when spec-agent needs to hand work to another agent in the standard repository format
metadata:
  author: starter-kit
  version: 0.3.0
  scope: Owns the standard handoff shape for spec-agent, but does not replace the governing spec or final implementation details
  auto-invoke: false
tools: shared/tool-permissions.md#standard-tool-set
---
# spec-agent Handoff
# Approved Intent Source
# Spec Summary
# Interfaces and Contracts
# Constraints
# Acceptance Criteria
# Validation Plan
