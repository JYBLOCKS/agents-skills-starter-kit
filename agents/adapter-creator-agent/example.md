---
name: adapter-creator-agent-example
description: Example invocation and output shape for adapter-creator-agent.
trigger: Use this when you need a concrete example of how adapter-creator-agent should be invoked or how it should respond.
metadata:
  author: starter-kit
  version: 0.3.0
  scope: Owns a reference example for adapter-creator-agent, but does not redefine the canonical contract.
  auto-invoke: false
tools: shared/tool-permissions.md#standard-tool-set
---

# adapter-creator-agent Example

## Phase 1: Intent

The user wants an adapter for a new coding tool.

## Phase 2: Contract Completion

The creator asks about discovery model, mapping rules, copy vs reference behavior, wrappers, and setup steps.

## Phase 3: Assembly and Validation

The output includes the adapter README, setup example, and validation against the adapter contract.