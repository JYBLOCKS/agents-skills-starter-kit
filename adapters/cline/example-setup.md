---
name: cline-example-setup
description: Example installation and bootstrap steps for cline.
trigger: Use this when you need a concrete install and bootstrap example for cline.
metadata:
  author: starter-kit
  version: 0.3.0
  scope: Owns an example install path for cline, but does not replace the adapter contract or canonical repository structure.
  auto-invoke: false
allowed-tools:
  - generic
  - openai
  - claude-code
  - opencode
  - cursor
  - windsurf
  - github-copilot
  - cline
  - gemini-cli
  - codex
---
# Cline Example Setup

1. Install with `npx skills add JYBLOCKS/agents-skills-starter-kit -a cline`.
2. Open the repository in Cline.
3. Attach the relevant rules, context files, and skills from the neutral core.
