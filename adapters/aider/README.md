# Aider Adapter Placeholder

This folder is a starter placeholder for a future Aider-specific adapter.

Use `adapters/generic/` as the baseline, then document:

- discovery model
- prompt/rules mapping
- copied vs referenced files
- minimum setup
