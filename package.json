{
  "name": "agents-skills-starter-kit",
  "version": "0.3.0",
  "private": true,
  "description": "Multi-tool agent and skill starter kit with portable distribution metadata and host-specific install adapters.",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "metadata:sync": "node scripts/add-metadata-frontmatter.mjs",
    "metadata:check": "node scripts/add-metadata-frontmatter.mjs --check",
    "validate:distribution": "node scripts/validate-distribution.mjs",
    "validate:docs": "node scripts/validate-docs.mjs",
    "validate": "npm run validate:distribution && npm run validate:docs"
  }
}
